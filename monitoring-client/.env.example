# Monitoring API endpoint (without trailing slash)
MONITOR_API_URL=https://app-monitoring-knowledgehub.azurewebsites.net/api/v1

# API Key (must match the API_KEY configured on the server)
MONITOR_API_KEY=
